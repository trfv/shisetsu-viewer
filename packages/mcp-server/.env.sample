# stdio エントリ（index.ts）/ CLI（cli.ts）用。
# API_ENDPOINT は D1 API のベース URL（例: https://api.shisetsudb.com）。
# ADMIN_API_KEY は stdio の write ツールが使う X-Admin-Key（CLI の read のみなら不要）。
API_ENDPOINT=
ADMIN_API_KEY=
